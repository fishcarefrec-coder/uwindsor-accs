# Node modules & build artifacts
node_modules/
dist/
build/
# Environment & Logs
.env
.env.*
# Keep every *.example template — `.env.*` above would otherwise swallow new
# ones (e.g. apps/web/.env.mobile.example) without any warning.
!.env.example
!.env.*.example
*.log

# Python virtualenvs & cache
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# OS files
.DS_Store
Thumbs.db

# Editor config. The VS Code Java extension writes .vscode/settings.json on its
# own once the Android/Gradle project is opened.
.vscode/

# Agent Customization & Documentation Exclusions
.agents/
.gemini/
.antigravity/
.claude/
CLAUDE.md
# docs/ is tracked (handover and design docs); only the university's scanned
# source PDFs stay out, because this repository is public.
docs/*.pdf

# Root Scratch & Helper Scripts
scratch/
check_rooms.py
fix_rooms.py
fix_stuck_projects.py
list_data.py
migrate_facility.py
reset_db.py
test_db.py
test_login.py
test_close.py
update_password.py


# Built APKs staged for distribution. Large binaries, and CI publishes the
# canonical signed copy to a GitHub Release — see .github/workflows/release-apk.yml.
dist-apk/
